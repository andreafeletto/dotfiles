#!/bin/sh

prefix="$HOME/.password-store"

files=$(find "$prefix" ! \( -path .git -prune \) -name \*.gpg -exec basename -s ".gpg" '{}' ';')

selected=$(echo "$files" | dmenu)

[ -n "$selected" ] || exit

pass otp code -c "$selected"
